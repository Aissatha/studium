name: stadium
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # --- App stack (mobile + scalable) ---
  supabase_flutter: ^2.8.0
  flutter_dotenv: ^5.1.0
  flutter_riverpod: ^2.6.1
  go_router: ^14.2.7
  file_picker: ^8.1.4
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
